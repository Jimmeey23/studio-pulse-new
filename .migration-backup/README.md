# Studio Pulse Only

Standalone Vite app that renders only the current Studio Pulse interface.

## Run

```bash
npm install
npm run dev
```

The app opens to `/studio-pulse` and redirects `/` there automatically.
